{
  "name": "@qualdesk/firestore-big-batch",
  "version": "0.0.2",
  "description": "A library to work around hard limit for Firestore Batched Writes",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "private": false,
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "yarn build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kat Lynch <kehlynch@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "@google-cloud/firestore": "^4.13.2",
    "typescript": "^4.3.5"
  },
  "repository": "github:qualdesk/firestore-big-batch",
  "keywords": [
    "firestore",
    "firebase",
    "typescript",
    "batch",
    "migration"
  ]
}
